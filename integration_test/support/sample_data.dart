// The sample content lives in `lib` so [DevSeeder] can write it into a real
// account in debug builds. Re-exported here so the screenshot run keeps its
// original import.
export 'package:paw_vault/core/dev/sample_data.dart';
